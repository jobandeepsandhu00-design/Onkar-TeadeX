# ── API Backend URL ─────────────────────────────────────────
# Set this in Vercel → Project Settings → Environment Variables
# Should point to your deployed API server (Railway / Render / etc.)
# No trailing slash.
VITE_API_URL=https://your-api.railway.app

# ── Vite build (set automatically by Vercel — do not change) ─
# NODE_ENV=production
