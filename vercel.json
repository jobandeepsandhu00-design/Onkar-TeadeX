{
  "buildCommand": "pnpm --filter @workspace/trading-os run build",
  "outputDirectory": "artifacts/trading-os/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 60,
      "includeFiles": "lib/**"
    }
  },
  "routes": [
    {
      "src": "/assets/(.*)",
      "headers": {
        "Cache-Control": "public, max-age=31536000, immutable"
      },
      "continue": true
    },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
